// ignore: unused_import
import 'dart:ffi';

class Transactions {
  String title;
  String province;
  double amount;
  DateTime date;

  Transactions(
      {required this.title,
      required this.province,
      required this.amount,
      required this.date});
}
